package main.commands;

import java.util.ArrayList;

public class Mode extends Command{
    public Mode(ArrayList<String> arguments, FileManager fileManager) {
        super(arguments, fileManager);
    }

    @Override
    public void execute() {

    }
}
